Chatbase
============================================================

A simple, lightweight clide-side chat app using Firebase and Google sign on.

Replace 'firebaseURL' in index.js with the path to your Firebase.
Firebase must be setup to allow Google authentication (Simple Login).
